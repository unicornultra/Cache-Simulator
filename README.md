# Cache-Simulator
This project implements a cache simulator that processes memory access traces to simulate cache behavior. It supports configurable parameters like cache size, associativity, and replacement policies, and outputs performance metrics such as hit and miss rates.
